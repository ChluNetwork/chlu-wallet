import { blue, borderColor, ratingColor } from 'context/palette'

export default {
  avatarStyle: {
    color: blue,
    backgroundColor: 'rgb(255, 255, 255)',
    style: { border: `1px solid ${borderColor}` }
  },
  ratingStyle: { starColor: ratingColor }
}
